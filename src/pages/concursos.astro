---
import Layout from "../layouts/Layout.astro";
import { readdirSync } from "node:fs";
import { basename, extname } from "node:path";

const flyerDir = new URL("../../public/Concursos", import.meta.url);
const flyerFiles = readdirSync(flyerDir)
    .filter((file) => /\.(png|jpe?g|webp|avif)$/i.test(file))
    .sort((a, b) => a.localeCompare(b, undefined, { sensitivity: "base" }));

const titleOverrides: Record<string, string> = {
    "cartografiado geologico subterraneo": "Cartografiado Geológico Subterráneo",
    "cartografiado geologico superficial": "Cartografiado Geológico Superficial",
    "debate geologico": "Debate Geológico",
    "elaboracion de columna estratigrafica y seccion geologica": "Elaboración de Columna Estratigráfica y Sección Geológica",
    "fotointerpretacion geologica": "Fotointerpretación Geológica",
    "fotografia geologica": "Fotografía Geológica",
    "geomecanica de rocas": "Geomecánica de Rocas",
    "logueo geologico": "Logueo Geológico",
    "reconocimiento paleontologico": "Reconocimiento Paleontológico",
    "trivia geologica": "Trivia Geológica",
};

// Map flyer key -> PDF filename inside /public/Concursos/PDFs
const pdfMap: Record<string, string> = {
    "cartografiado geologico subterraneo": "Brochure del Concurso de Cartografiado Geológico Subterráneo.pdf",
    "cartografiado geologico superficial": "Brochure del Concurso de Cartografiado geológico superficial.pdf",
    "debate geologico": "Brochure del Concurso de Debate Geológico.pdf",
    "elaboracion de columna estratigrafica y seccion geologica": "Brochure del Concurso de Levantamiento de Columna Estratigráfica.pdf",
    "fotografia geologica": "Brochure del Concurso de Fotografía geológica.pdf",
    "fotointerpretacion geologica": "Brochure del Concurso de Fotointerpretación.pdf",
    "geomecanica de rocas": "Brochure del Concurso de Geomecánica de rocas.pdf",
    "logueo geologico": "Brochure del Concurso de Logueo Geológico.pdf",
    "reconocimiento de minerales": "Brochure del Concurso Reconocimiento de Minerales.pdf",
    "reconocimiento de rocas": "Brochure del Concurso de Reconocimiento de Rocas.pdf",
    "reconocimiento paleontologico": "Brochure del Concurso de Reconocimiento Paleontológico.pdf",
    "trivia geologica": "Brochure del Concurso de Trivia Geológica.pdf",
};

const normalizeKey = (text: string) =>
    text
        .toLowerCase()
        .normalize("NFD")
        .replace(/\p{Diacritic}/gu, "")
        .replace(/\s+/g, " ")
        .trim();

const flyers = flyerFiles.map((file) => {
    const name = basename(file, extname(file));
    const cleaned = name.replace(/[_]+/g, " ").replace(/\s+/g, " ").trim();
    const key = normalizeKey(cleaned);
    const pretty = titleOverrides[key]
        ? titleOverrides[key]
        : cleaned.length > 0
              ? cleaned
                    .split(" ")
                    .map((word) => (word ? word[0].toUpperCase() + word.slice(1).toLowerCase() : ""))
                    .join(" ")
              : "Flyer";

    const pdfFile = pdfMap[key];
    return {
        key,
        title: pretty,
        src: `/Concursos/${encodeURI(file)}`,
        pdfHref: pdfFile ? `/Concursos/PDFs/${encodeURI(pdfFile)}` : undefined,
    };
});
---

<Layout title="Concursos - CONAEINGEO 2026">
    <section class="page-header concursos-hero">
        <div class="container">
            <h1>Galería de concursos</h1>
            <p class="lead">
                Únete al espíritu competitivo de los concursos oficiales del XV CONAEINGEO: cartografía, 
                debate, reconocimiento geológico, fotografía, modelamiento y más.
            </p>
        </div>
    </section>

    <section class="section flyers-section">
        <div class="container">
            {flyers.length > 0 ? (
                <div class="flyers-grid">
                    {flyers.map((flyer) => (
                        <article class="flyer-card">
                            <div class="flyer-frame">
                                <img src={flyer.src} alt={`Flyer ${flyer.title}`} loading="lazy" decoding="async" />
                            </div>
                            <div class="flyer-meta">
                                <h3>{flyer.title}</h3>
                                {!flyer.pdfHref && <p>Detalles próximamente</p>}
                                {flyer.pdfHref && (
                                    <div class="flyer-actions">
                                        <a
                                            class="flyer-download-btn"
                                            href={flyer.pdfHref}
                                            target="_blank"
                                            rel="noopener noreferrer"
                                        >
                                            Descarga las bases del concurso
                                        </a>
                                    </div>
                                )}
                            </div>
                        </article>
                    ))}
                </div>
            ) : (
                <p class="empty-state">Estamos preparando los flyers para esta sección.</p>
            )}
        </div>
    </section>
</Layout>

<style>
    .concursos-hero {
        background: var(--gradient-hero);
        color: var(--color-white);
        padding: 8rem 0 4rem;
        text-align: center;
        margin-top: 80px;
    }

    .concursos-hero .container {
        position: relative;
        z-index: 1;
        text-align: center;
        max-width: 900px;
    }

    .concursos-hero h1 {
        color: var(--color-white);
        margin-bottom: 1rem;
    }

    .eyebrow {
        letter-spacing: 0.3em;
        text-transform: uppercase;
        font-size: 0.85rem;
        margin-bottom: 1rem;
        color: rgba(255, 255, 255, 0.85);
    }

    .concursos-hero .lead {
        font-size: 1.25rem;
        line-height: 1.7;
        color: rgba(255, 255, 255, 0.9);
        max-width: 600px;
        margin: 0 auto;
    }

    .flyers-section {
        background: var(--color-lighter);
    }

    .flyers-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 2.5rem;
        margin: 2rem auto 0;
        max-width: 1200px;
    }

    .flyer-card {
        background: var(--color-white);
        border-radius: var(--radius-lg);
        padding: 1.25rem;
        box-shadow: var(--shadow-md);
        display: flex;
        flex-direction: column;
        gap: 1rem;
        border: 1px solid rgba(30, 58, 138, 0.12);
        transition: transform var(--transition-fast), box-shadow var(--transition-fast);
    }

    .flyer-card:hover {
        transform: translateY(-6px);
        box-shadow: var(--shadow-xl);
    }

    .flyer-frame {
        flex: 1;
        min-height: 420px;
        border-radius: var(--radius-md);
        overflow: hidden;
        background: var(--color-light);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .flyer-frame img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        display: block;
        transition: transform var(--transition-slow);
    }

    .flyer-card:hover .flyer-frame img {
        transform: scale(1.02);
    }

    .flyer-meta h3 {
        font-size: 1.1rem;
        color: var(--color-primary);
        margin-bottom: 0.35rem;
    }

    .flyer-meta p {
        margin: 0;
        font-size: 0.9rem;
        color: var(--color-text-light);
    }

    .flyer-actions {
        margin-top: 0.75rem;
    }

    .flyer-download-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.6rem 1.2rem;
        border-radius: var(--radius-md);
        border: 2px solid var(--color-primary);
        background: var(--color-primary);
        color: var(--color-white);
        font-size: 0.95rem;
        font-weight: 600;
        text-decoration: none;
        transition: transform var(--transition-fast), box-shadow var(--transition-fast);
    }

    .flyer-download-btn:hover,
    .flyer-download-btn:focus-visible {
        transform: translateY(-1px);
        box-shadow: var(--shadow-md);
    }

    .empty-state {
        text-align: center;
        padding: 3rem 0;
        font-size: 1.1rem;
        color: var(--color-text-light);
    }

    @media (max-width: 768px) {
        .flyer-frame {
            min-height: 280px;
        }

        .flyers-grid {
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        }
    }
</style>
